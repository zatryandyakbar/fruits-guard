<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/darkOrange" /> <!-- Warna latar -->
    <stroke
        android:width="2dp"
        android:color="@color/orange" /> <!-- Warna border -->
    <corners android:radius="8dp" /> <!-- Sudut melengkung -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>